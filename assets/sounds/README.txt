Sound effects are currently synthesized in code (see playTone() and the sfx*()
functions near the top of game.js) using the Web Audio API — no files needed.

If you want richer sound later, drop .mp3 or .ogg files here and swap the
playTone() calls for an <audio> element per effect.
